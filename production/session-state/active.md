# Steam Vertical Slice Session

- Concept: `西西弗斯下山` Steam-directed production path.
- Source concept: `/Users/haodong/Documents/GitHub/PKM/PlayWithExperiences/Normal Markdown - Shared/Game/Idea/game of sisyphus/2025-1019-1236  西西弗斯下山 game of sisyphus.md`
- Current objective: build toward a Steam-releasable Godot game through a verified vertical slice, then alpha/beta/release gates.
- Current milestone: Milestone 2 - Vertical Slice Loop.
- Current implementation slice:
  - Level 4/5 storm-loop design target defined in `design/gdd/game-concept.md`.
  - Steam roadmap defined in `production/milestones/steam-roadmap.md`.
  - Implementation plan defined in `docs/superpowers/plans/2026-05-24-sisyphus-steam-vertical-slice.md`.
  - Logic foundation implemented: `LevelDefinition`, `RunMetrics`, `LevelManager`, `TrailRecorder`, `EnvironmentResponse`, `WeatherController`, `HummingController`.
  - Logic tests added for level config, metrics, no-teleport release/descent, trail response, and humming clarity.
  - `VerticalSlice.tscn` assembled as a loadable Level 4 storm-loop scene skeleton with player, stone, mountain, weather, trail, environment response, and humming systems.
  - `MainMenu.tscn` is now the exported app boot scene; Play enters `VerticalSlice.tscn`, while `Main.tscn` remains available as a development prototype scene.
  - `VerticalSlice.tscn` now has a pause menu: `Esc`/`P` pauses, Resume unpauses, Main Menu returns to `MainMenu.tscn`, and Quit exits.
  - `test_vertical_slice_smoke.gd` added for the level loop contract.
  - `--vs-auto=<mode>` visual verification modes added for approach, push, left, right, release, descent, and hum.
  - `--vs-auto=loop` added as a connected short-loop driver from approach through hum reward.
  - `--vs-auto=route` added as a front-base bot smoke path that keeps player contact while stone/release/descent/trail/response use real scene physics.
  - Push-exit visual reset added so descent/hum states clear stale push contact, hide first-person hands, and restore third-person arms to idle.
  - `test_vertical_slice_route_physics.gd` added to prove near-ridge and front-base continuous contact push can physically enter release/descent and generate trail response without teleporting the player.
  - `test_vertical_slice_route_physics.gd` now also proves default front-base player input by holding `W` can reach descent/complete and build trail response.
  - `RouteTelemetry.gd` and `HUD/Telemetry` added for hand-play readiness: contact ratio, contact loss count, max player-stone distance, max spin ratio, phase, and elapsed time.
  - `RouteTelemetry.gd` now produces `PROCEED / PIVOT / KILL`; current automated player-input route passes `PROCEED`.
  - `RouteTelemetry.gd` only counts max contact distance while pushing, so descent/complete free-walk distance no longer corrupts the push gate.
  - Core-loop priority reset: save/profile/settings/export work is paused until push-lab hand feel is strong enough to justify vertical-slice expansion.
  - 2026-05-27 push-feel correction: push camera/contact targeting now uses the player's aim directly instead of back-solving the camera from the contact point; during push, the camera centers the selected pressure point, first-person forearm starts are shortened/bounded, and the vertical slice shares the same reticle-centered contact model as PushLab.
  - `production/qa/playtests/push-intent-diagnostic-2026-05-27-reticle-camera-fix.md` records the refreshed push-intent diagnostic after this correction: `PROCEED`, contact delta `0.80`, force delta `88.71`, drift gap `1.56`.
  - `tests/test_push_lab_player_loop.gd` added as a real PlayerController/Input/RigidBody regression gate, so push-lab no longer relies only on idealized auto-positioned push tests.
  - `PushLab.gd` now starts the stone on the front staging flat and keeps it frozen until the first real push input, preventing the lab stone from rolling away before contact.
  - `PushController.gd` adds release rolling resistance during ascent when no hand contact is active, reducing post-release coasting so sustained input matters.
  - `PushController.gd` now uses `push_force_ramp_seconds`; contact push starts below full force and ramps up only while the player keeps holding push contact.
  - `PlayerController.gd` tracks `push_contact_seconds` and resets it on release/contact loss, so the force ramp is tied to actual sustained player input.
  - `PlayerController.gd` now enforces a minimum body-vs-stone contact shell, preventing the CharacterBody from being moved directly into the RigidBody stone.
  - `PlayerController.gd` now treats `W` during ascent as push/reacquire intent: if contact is lost near the stone, player movement steers back toward the downhill hand-contact position instead of sprinting away uphill.
  - `PlayerController.gd` now gates reacquire steering by camera aim: holding `W` while looking away no longer magnet-pulls the player back to the stone, while aiming toward the contact point still lets the player reapproach.
  - `PlayerController.gd` now keeps an actively pushing player on the stone's downhill contact side, preventing the body from overtaking the stone and breaking the full-route push loop.
  - `PushController.gd` now applies direction-sensitive release resistance: uphill coasting is braked hard after release, while downhill rollback keeps weaker damping so slope mistakes can actually roll the stone back.
  - `GameState.gd` now requires the player to return near the low rolled stone before entering `complete`; reaching the back foot alone no longer completes the loop.
  - `GameState.gd` now delays `ascent -> release -> descent` until the stone is actually committed past the ridge onto the back slope, instead of releasing at the crest lip where it can roll back to the front side.
  - `VerticalSlice.gd` now samples push telemetry only during `ASCENT`, so walking downhill with `W` no longer corrupts the hand-play push gate.
  - `tests/test_push_lab_player_loop.gd` now includes a mid-slope release regression: after a short sustained push and release, the stone must roll back downhill under gravity.
  - `tests/test_push_lab_player_loop.gd` now includes a bad side-contact regression: half-aimed side contact should not hold valid push frames and should allow rollback on the slope.
  - `PushLab.gd` now exposes a derived `push_motion_state()` and shows `State push/coast/stall/rollback/idle` in the HUD, so release and rollback are readable from real contact and velocity instead of inferred only from force numbers.
  - `tests/test_push_lab_player_loop.gd` now asserts that a released mid-slope stone visibly enters `rollback` state after the player lets go.
  - `FirstPersonHandsController.gd` now accepts push motion feedback and exposes `feedback_state()` / `feedback_tint()`; `PlayerController.gd` drives this from real contact and uphill velocity so rollback can pull/sink the hand targets and warm the hand tint without relying on HUD text.
  - `tests/test_push_lab_player_loop.gd` now verifies that released mid-slope rollback propagates to first-person hands as a non-HUD cue.
  - `PushLab.gd` now exposes `enter_first_person_push_observation()` and `first_person_push_camera_status()`, giving tests and manual checks a close first-person push camera that hides the third-person body, shows hands, and keeps the camera close to hand contact and stone.
  - `PushLab.gd` now reports first-person hand/forearm camera-space layout metrics, and `test_push_lab.gd` requires both hands and forearms to stay on opposite sides of the center contact point so the stone remains readable in push view.
  - `PushLab.gd` now positions the observation camera before calling `FirstPersonHandsController.gd`; `FirstPersonHandsController.gd` also widens/lower-starts the first-person arms and enforces minimum two-hand spread around the contact point.
  - `PushLab.gd` now has `save_observation_snapshot(path)` for first-person push evidence. GUI runs save the actual Viewport; headless runs save a schematic placeholder using the same layout metrics, avoiding dependence on macOS window screenshots.
  - `PushLab.gd` now has `save_core_evidence_sequence(base_path)` for the minimum push-feel evidence set: approach, normal push, left-biased push, right-biased push, and release/rollback. The sequence records motion state, contact validity, and writes one PNG per state.
  - `PushLab.gd` now backs the lab-only first-person observation camera away as lateral contact bias increases, without changing `PushController.gd` contact-force physics. `test_push_lab.gd` guards left/right bias readability with camera distance and camera-cone ratios.
  - `PushLab.gd` evidence now exposes `contact_side_offset` and `force_side_component`; `test_push_lab.gd` verifies left/right aim move contact across the stone and create opposite playable side force.
  - `test_game_logic.gd` now verifies descent completion requires returning to the stone, preserving the intended "walk back to the boulder" loop.
  - `test_vertical_slice_route_physics.gd` now proves a real player-input route can push from the front base, cross the ridge, steer downhill toward the rolled stone, and enter `complete`.
  - Design clarification: the first loop should end after the player descends and returns to the stone; any next push belongs to a later chapter/transition that is still TBD, so same-scene back-side re-push is not a current target.
  - `design/quick-specs/chapter-i-end-boundary-2026-05-26.md` records this as a CCGS quick-spec boundary: `complete / Chapter I End` is the Chapter I stop point, not a same-scene Chapter II placeholder or auto-reset.
  - `VerticalSlice.gd` now exposes a `complete` visual mode, the connected visual loop ends at `complete`, and the HUD switches from push instructions to a Chapter I completion prompt at the first-loop ending.
  - `PlayerController.gd` now treats `complete` as an end-of-chapter hold state: movement and push input are ignored, push engagement is cleared, and the camera blends back out of first-person push view until a future transition system takes over.
  - `VerticalSlice.gd` now has a minimal chapter-end hold shell: `chapter_end_status()` exposes Chapter I End, hold elapsed time, and "next punishment pending"; `advance_chapter_end_hold()` moves the camera into a reflective stone/path composition without starting Chapter II.
  - `VerticalSlice.tscn` now includes low-cost Chapter End visual markers: multiple narrow dark storm shards and a small warm light crack, so the ending reads as "the gods remain hostile, but not everything is under their control" without using one giant debug wall.
  - Manual playtest snapshots saved from `complete` now include `Chapter End Evidence`, so reports distinguish the first-loop ending from an unresolved next-level transition.
  - `production/qa/playtests/playtest-2026-05-25-chapter-end-visual.png` records earlier GUI visual evidence of the Chapter I End state; Computer Use later observed the revised shard composition in the Godot window, while macOS `screencapture` still captured the Codex app instead of the raised Godot window.
  - `tests/test_push_lab_player_loop.gd` now includes a sustained camera-bias route-pressure regression: a right-aim push on the mid-slope must drift visibly more than straight push while remaining recoverable.
  - `MountainBuilder.gd` now places some front-slope obstacles closer to the channel edge while keeping the stone center corridor clear; obstacle mesh/collider nodes are explicitly named for inspection.
  - `test_game_logic.gd` now verifies obstacle edge pressure on both sides, varied obstacle radii, and passable stone center clearance.
  - `test_push_lab.gd` now verifies PushLab obstacles are instantiated as `StaticBody3D` objects with collision shapes, not only visual stones.
  - `tests/test_push_lab_player_loop.gd` now includes an obstacle-glance regression: a biased push must actually glance a near-channel obstacle, slow/deflect without launching the stone, then recover uphill with straight push.
  - `PushLab.gd` now exposes `evaluate_bias_recovery_route(side_sign)`, so the lab itself can run the short "bias into near-channel obstacle, slow down, straighten, recover uphill" route on either side and return touch, air-gap, speed, recovery, channel, distance, and spin metrics without relying on hidden test helpers.
  - `test_push_lab.gd` now guards that evaluator on both left and right bias routes: each side must touch the obstacle, avoid launch, slow on impact, recover uphill, stay in the clear channel, move through real physics, and avoid in-place spin.
  - `PlaytestReportBuilder.gd` added to generate CCGS-style Markdown playtest reports from telemetry.
  - `production/qa/playtests/README.md` added as the manual vertical-slice playtest protocol and report path guide.
  - `tests/generate_auto_route_playtest_report.gd` added to generate an automated route baseline report from real scene simulation.
  - `RouteTelemetry.gd` now separates `Push Gate` from `Slice Gate`: stable contact can be `PROCEED` while a compressed automated route remains `PIVOT` until a representative 5-10 minute loop is validated.
  - `Tuning.gd` now owns the representative slice timing window (`representative_slice_min_seconds` / `representative_slice_max_seconds`, default 5-10 minutes), and `RouteTelemetry.gd` uses those values instead of hard-coded pacing constants.
  - `tests/generate_auto_route_playtest_report.gd` now waits for `complete` instead of stopping at `descent`, so the automated baseline covers the whole first loop ending.
  - `PlaytestReportBuilder.gd` now prints phase duration breakdowns, so pacing work can target the actual short phase instead of only seeing aggregate loop time.
  - `PlaytestReportBuilder.gd` and the auto-route report now name `complete` as the first-loop ending point: push uphill, release, descend, return to stone, then stop; the next-chapter transition remains a design TODO.
  - An attempted route-smoke pacing tune by weakening the standard push baseline was reverted because it risked breaking core push physics. Current route smoke is a compressed stability baseline, not representative pacing evidence.
  - `production/qa/playtests/playtest-2026-05-24-auto-route.md` records the current automated baseline: final phase `complete`, `Push Gate PROCEED`, `Slice Gate PIVOT`, 31.8s total, 26.7s ascent, 4.8s descent, 100% contact, 1 contact loss, 2.57 max distance, 1.1 max spin ratio, 0.80 hum clarity, and no route teleports after start.
  - `PlaytestReportBuilder.gd` now accepts a PushLab bias-gate evidence block and prints `Push Lab Bias Gate`, reason, and left/right recovery metrics in the quantitative section.
  - `tests/generate_auto_route_playtest_report.gd` now runs `PushLab.evaluate_bias_recovery_route(-1.0)` and `evaluate_bias_recovery_route(1.0)` before writing the automated route report, so the baseline records both full-route telemetry and core hand-contact steerability.
  - An initial combined `playtest-2026-05-26-auto-route.md` run exposed telemetry contamination: route duration inflated to 44.9s because PushLab validation ran while the completed VerticalSlice still processed physics.
  - `tests/generate_auto_route_playtest_report.gd` now freezes the completed VerticalSlice before running the external PushLab gate, so PushLab validation no longer inflates route duration or `complete` phase seconds.
  - `production/qa/playtests/playtest-2026-05-26-auto-route.md` was regenerated after that fix: the combined baseline still has `Push Gate PROCEED`, `Slice Gate PIVOT`, and `Push Lab Bias Gate PROCEED`, while route duration is back to 31.8s and `Complete Duration` is 0.0s.
  - `VerticalSlice.gd` route smoke now includes a mid-ascent right-bias pressure segment followed by center recovery, tracked by `route_smoke_max_lateral_offset` and `route_smoke_bias_recovered`, so the main slice route itself exercises aim-biased pushing instead of relying only on PushLab.
  - `test_vertical_slice_route_physics.gd` now requires the route smoke to expose a visible lateral offset and recover from that bias before descent.
  - `production/qa/playtests/playtest-2026-05-26-auto-route.md` was regenerated after adding route-smoke pressure: duration is 32.4s, `Route Smoke Max Lateral Offset` is 1.23, `Route Smoke Bias Recovered` is Yes, `Push Gate` remains `PROCEED`, `Slice Gate` remains `PIVOT`, and `Push Lab Bias Gate` remains `PROCEED`.
  - `RouteTelemetry.gd` now records push effort evidence: uphill push distance, average uphill speed, minimum uphill speed, and slow-push duration, so the route report can distinguish "stable contact" from "measurable labor."
  - `RouteTelemetry.gd` now separates `Burden Gate` from the contact-focused `Push Gate`, so stable rolling contact can remain `PROCEED` while too-smooth transport is correctly marked `PIVOT`.
  - `PlaytestReportBuilder.gd` now prints those push effort metrics plus `Burden Gate` in the quantitative data section, and `HUD/Telemetry` shows compact `Effort` and `Burden` readouts.
  - `PushController.gd` now shapes hand-contact force with a burden speed governor and hand-stride force variation, so sustained pushing no longer behaves like smooth motor transport while still using contact-point force on the RigidBody.
  - `Tuning.gd` now exposes burden feel parameters per preset: target uphill speed, speed governor strength, hand-stride force depth, and minimum force scale.
  - `RouteTelemetry.gd` now counts low-speed labor at the controlled-burden level, not only near-stall frames, so `Slow Push Duration` better reflects heavy uphill work.
  - `production/qa/playtests/playtest-2026-05-26-auto-route.md` was regenerated after burden tuning: uphill push distance 37.13m, average uphill speed 0.77m/s, minimum uphill speed 0.01m/s, slow-push duration 22.2s, `Push Gate PROCEED`, `Burden Gate PROCEED`, `Slice Gate PIVOT`, and `Push Lab Bias Gate PROCEED`. This moves the current automated baseline from stable-but-too-smooth to stable-with-measurable-labor, while preserving the first-loop `complete` ending and keeping the representative 5-10 minute slice unproven.
  - `Tuning.gd` now exposes vertical-slice pacing profiles: `smoke` for fast automated regressions and `representative` for the long 5-10 minute route target.
  - `VerticalSlice.gd` supports `--slice-pacing=representative` and `apply_pacing_profile("representative")`, rebuilding the same mountain with an extended ascent, extended descent, and denser obstacle pressure while preserving the first-loop Chapter I `complete` ending.
  - `MountainBuilder.gd` now increases z mesh subdivisions with route depth so representative-length routes do not become a sparse stretched mesh.
  - `tests/generate_auto_route_playtest_report.gd` supports `--slice-pacing=<profile>` and report output now records both `Pacing Profile` and `Estimated Pacing Loop`. The default auto report remains `smoke` to keep routine regressions fast; representative runs are intended for scheduled long-route human validation.
  - Design boundary confirmed again: after the player finishes one push cycle and descends to the rolled stone, the first chapter should end. The next push belongs to Chapter II or another transition, which remains intentionally unresolved.
  - A 2026-05-26 attempt to generate a representative headless auto-route report was stopped after several minutes because it was not an acceptable iteration loop and produced no report artifact. Representative pacing is therefore still unvalidated; keep `smoke` as the automated regression path and use `representative` primarily for scheduled human/visual playtests.
  - `tests/generate_auto_route_playtest_report.gd` now supports bounded diagnostics through `--max-auto-route-frames=<frames>` and `--skip-push-lab-gate`, and reports `Route Complete`, `Route Status Reason`, and `Route Frames Run` so incomplete representative checks produce evidence instead of silent long runs.
  - `Tuning.gd` now exposes explicit burden gate tuning (`burden_slow_speed_threshold`, `burden_min_slow_ratio`, `burden_fast_average_speed`) and the representative pacing profile uses a slower target speed, stronger speed governor, lower minimum force scale, and stricter slow-labor threshold without changing the smoke profile.
  - `RouteTelemetry.gd` now uses the active tuning for slow-push counting and `hud_summary(tuning)`, so HUD/report verdicts no longer disagree when representative burden thresholds differ from smoke defaults.
  - `production/qa/playtests/playtest-2026-05-26-auto-route-representative-diagnostic.md` records the current bounded 1800-frame representative diagnostic: final phase `ascent`, `Route Complete: No`, `Route Status Reason: frame budget exhausted before complete`, `Push Gate PROCEED`, `Burden Gate PROCEED`, `Slice Gate PIVOT`, average uphill speed 0.90m/s, and slow-push duration 7.4s. This fixes the representative front-segment burden regression but still does not validate the full 5-10 minute Steam vertical-slice loop.
  - `tests/generate_auto_route_playtest_report.gd` and `VerticalSlice.gd` now support route checkpoints (`front`, `mid`, `ridge`, `descent`) for representative diagnostics. These are explicitly diagnostic and do not replace a continuous `front -> complete` run.
  - `production/qa/playtests/playtest-2026-05-26-auto-route-representative-ridge.md` records a bounded `ridge` checkpoint: it reaches `descent`, generates response points and generated hum, and keeps `Push Gate PROCEED` / `Burden Gate PROCEED`, but does not reach `complete` within 1800 frames.
  - `production/qa/playtests/playtest-2026-05-26-auto-route-representative-descent.md` records a bounded `descent` checkpoint: it reaches `complete` in 153 frames from a low-back-slope start. This proves the final walk-back completion shell works in isolation, while the continuous long descent from ridge to low stone remains unverified.
  - `tests/generate_auto_route_playtest_report.gd` now separates `Route Complete` from `Checkpoint Complete`, so checkpoint diagnostics can prove their segment target without being mistaken for full representative-slice proof.
  - `production/qa/playtests/playtest-2026-05-26-auto-route-representative-ridge-long.md` records a stronger late-route diagnostic: starting from the `ridge` checkpoint, it reaches `complete` in 103.8s / 6226 frames with `Push Gate PROCEED`, `Burden Gate PROCEED`, response points, `Hum Clarity 0.80`, and generated hum. This proves `ridge -> release -> long descent -> complete` can run continuously, while the full `front -> complete` representative run is still unvalidated.
  - A full representative `front -> complete` run initially failed after 42000 frames because the route-smoke bias window was tied to ascent percentage; on the long route, that over-amplified lateral pressure, drove the stone to `Route Smoke Max Lateral Offset 13.28`, broke contact, and left the run in `ascent` with `Push/Burden/Slice KILL`.
  - `VerticalSlice.gd` now uses a fixed ascent-distance route-smoke bias window and early recovery check instead of percentage-of-route windows. `test_vertical_slice_route_physics.gd` now verifies representative route smoke still creates visible lateral pressure, stays in the recoverable channel, recovers early, and keeps player/stone coupled.
  - `production/qa/playtests/playtest-2026-05-26-auto-route-representative-full.md` now records the first complete representative automated baseline from `front`: `Duration 381.2s`, `Final Phase complete`, `Route Complete Yes`, `Push Gate PROCEED`, `Burden Gate PROCEED`, `Slice Gate PROCEED`, contact 100%, 1 contact loss, max distance 2.57, max spin ratio 1.0, uphill push distance 246.06m, average uphill speed 0.86m/s, slow-push duration 140.4s, ascent 286.2s, descent 94.4s, response points 539, hum clarity 0.80, generated hum yes, no route teleports, max lateral offset 1.36, and bias recovered yes. PushLab was intentionally skipped in this long run, so use the separate PushLab/smoke evidence for bias-gate coverage.
  - `HummingController.gd` now generates an internal `AudioStreamWAV` hum phrase from code with no external audio asset.
  - `VerticalSlice.tscn` includes `HumPlayer`; descent/hum reward states play the generated hum stream.
  - Headless tests generate hum streams but no longer call `AudioStreamPlayer.play()`, avoiding leaked `AudioStreamWAV` / `AudioStreamPlaybackWAV` instances at test shutdown while keeping GUI playback enabled.
  - `HummingController.gd` now exposes chapter-end audio mix state: the storm remains audibly present, the generated hum resists it, and clear/fragmented runs produce different hum presence without using external audio.
  - Manual playtest snapshots saved from `complete` now include the Chapter End audio beat, storm audio presence, hum presence, and whether the hum overpowers the storm.
  - `VerticalSlice.gd` now supports `F9` manual playtest snapshot saving, with `--playtest-report-path` and `--playtest-tester-id` command-line overrides; each snapshot writes both Markdown and a same-name PNG screenshot.
  - `production/qa/playtests/playtest-2026-05-24-codex-visual.md` and `.png` record a GUI route run saved through `F9`: `complete`, `PROCEED`, 100% contact, 1 contact loss, 2.57 max push distance, 1.1 max spin ratio, 0.80 hum clarity.
  - `prototypes/vertical-slice-loop/REPORT.md` records Task 10 evidence and verdict.
  - Desktop export readiness has started: `godot/export_presets.cfg` defines macOS, Windows Desktop, and Linux presets targeting `builds/desktop/`, and `tests/test_export_readiness.gd` guards the configuration.
  - `tools/export_desktop.sh` adds repeatable desktop export commands plus a template preflight; it defaults to the locally verifiable macOS build, while Windows/Linux require target-machine validation before counting as release evidence.
  - `tools/smoke_macos_build.sh` unpacks `builds/desktop/macos/SisyphusDownhill.zip` and launches the exported app headlessly with an explicit log path.
  - `tools/run_representative_playtest.sh` now launches the representative `VerticalSlice.tscn` route for human validation and wires F9 snapshots to `production/qa/playtests/playtest-YYYY-MM-DD-tester-id.md`; `test_release_tooling.gd` guards the script contract, and `test_vertical_slice_playtest_capture.gd` verifies F9 report/screenshot capture still works.
  - 2026-05-26 follow-up verification for the Chapter I ending boundary passed: `test_vertical_slice_visual_modes.gd`, `test_vertical_slice_playtest_capture.gd`, `test_player_behavior.gd`, and `test_vertical_slice_route_physics.gd` all passed after adding the quick spec. This confirms the current code still treats `complete` as the first-loop ending, locks same-scene re-push, records Chapter End evidence, and preserves the full push-up / roll-down / walk-back route.
  - `PlaytestReportBuilder.gd` now adds a `Human Feel Gate` to F9/manual reports: subjective burden, camera pressure, aim/contact control, release/descent contrast, Chapter I End intentionality, and a required `Human Verdict: PROCEED/PIVOT/KILL`. `production/qa/playtests/README.md` now requires this gate for representative human evidence, so the next playtest can make a milestone decision instead of only collecting screenshots.
  - `HumanPlaytestReportValidator.gd`, `tests/validate_human_playtest_report.gd`, and `tools/validate_human_playtest_report.sh` now enforce that representative human reports cannot count as milestone evidence until the `Human Feel Gate` is filled with valid Yes/No/Partially answers, a `Human Verdict`, and a reason. The wrapper uses an explicit `.tmp/human-playtest-report-validator.log` path to avoid the Godot headless rotated-log crash seen when launched from the root helper script. Running it against the existing automated representative report correctly fails because that report has no human gate.
  - `VerticalSliceGateChecker.gd`, `tests/check_vertical_slice_gate.gd`, and `tools/check_vertical_slice_gate.sh` now scan `production/qa/playtests` and report the actual milestone gate from filled human reports. Current scan result is `VERTICAL_SLICE_GATE=PIVOT`, `VALID_HUMAN_REPORTS=0`, `INVALID_OR_AUTOMATED_REPORTS=8`, reason `no filled human playtest report with Human Feel Gate`; this is the correct current state despite the automated representative `PROCEED` route.
  - `tools/check_vertical_slice_gate.sh --require-proceed` now runs the same scan in strict milestone mode: current real QA reports still print `VERTICAL_SLICE_GATE=PIVOT` but exit with code `1`, so CI/release-gate usage cannot accidentally advance on status-only PIVOT evidence. Non-strict mode remains available for status reporting and exits `0` unless the gate is `KILL`.
  - `HumanPlaytestReportValidator.gd` now also rejects contradictory manual `PROCEED` reports: a human `PROCEED` is valid only when the same report shows `Pacing Profile: representative`, `Phase At End: complete`, and `Push Gate` / `Burden Gate` / `Slice Gate` all `PROCEED`. This prevents a filled subjective gate from overriding failed or compressed objective telemetry.
  - `tools/check_milestone2_ready.sh` now wraps the Milestone 2 exit gate: it runs the report validator tests, vertical-slice gate tests, report builder tests, release tooling contract, headless startup, then strict `check_vertical_slice_gate.sh --require-proceed`. The full command currently passes all automated checks and fails only at the intended human gate (`VERTICAL_SLICE_GATE=PIVOT`, `VALID_HUMAN_REPORTS=0`, exit code `1`). Explicit `.tmp/milestone2-*.log` paths avoid the Godot rotated-log crash during headless subchecks.
  - `tools/write_milestone2_readiness_report.sh` now writes `production/qa/milestone2-readiness.md`; current generated report records `Gate: PIVOT`, reason `no filled human playtest report with Human Feel Gate`, valid human reports `0`, invalid/automated reports `8`, and explicitly preserves `complete / Chapter I End` as the first-loop stop point instead of a same-scene Chapter II placeholder.
  - `tools/run_representative_playtest.sh` now continues after the Godot session exits: it validates the saved F9 report with `validate_human_playtest_report.sh` and then runs strict `check_vertical_slice_gate.sh --require-proceed`, so a human playtest cannot quietly finish without immediate report/gate feedback.
  - `tools/run_representative_playtest.sh --preflight <tester-id> <date>` now runs the F9 capture contract test, human report validator test, and readiness report writer before launching any GUI playtest. The 2026-05-26 preflight for `manual-01` passed and printed the exact human-run command plus the planned F9 report path.
  - `tools/run_representative_playtest.sh` now prints the `Push-Feel Retest Focus` checklist during preflight and before launching the representative GUI run, so the tester sees the latest hand/camera/contact checks without opening the worksheet separately.
  - `HumanPlaytestTriageBuilder.gd`, `tests/write_human_playtest_triage.gd`, `tests/test_human_playtest_triage_builder.gd`, and `tools/triage_human_playtest_report.sh` now turn a filled F9 report into a focused triage note. The triage separates invalid reports from milestone decisions, routes `PIVOT` to exactly one core-loop focus such as sustained burden/camera/aim/release/Chapter I ending, blocks content expansion on `KILL`, and routes valid `PROCEED` to the strict Milestone 2 gate. `run_representative_playtest.sh` now writes triage after a report exists, even before the strict milestone gate.
  - `production/qa/playtests/representative-human-playtest-worksheet.md` now provides a short live observer sheet for the representative human run: setup, no-coaching note, moment-by-moment observations, F9 end capture, Human Feel Gate fields, post-run validation/triage/gate commands, and PROCEED/PIVOT/KILL decision rules. `test_release_tooling.gd` guards that the worksheet continues to include the core evidence path.
  - `production/qa/playtests/representative-human-playtest-worksheet.md` now includes a dedicated `Push-Feel Retest Focus` block for the latest hand/camera/contact fixes: transition sanity, hand surface, look-down check, peripheral read, aim bias retest, and disengage/re-engage. `test_release_tooling.gd` guards those prompts so the next human gate explicitly tests the core push-feel complaints instead of only checking the final phase.
  - `PlaytestReportBuilder.gd` now writes the same `Push-Feel Retest Focus` table into F9/manual playtest reports, so the saved report itself asks the tester to fill transition sanity, hand surface, look-down, peripheral read, aim bias, and disengage/re-engage notes before the Human Feel Gate verdict.
  - `HumanPlaytestReportValidator.gd` now requires every `Push-Feel Retest Focus` row to be filled with `Yes` or `No` before a manual report can count as valid human evidence. This prevents a report from advancing Milestone 2 by filling only the high-level Human Feel Gate while skipping the latest hand/camera/contact checks.
  - `HumanPlaytestReportValidator.gd` now rejects contradictory manual `PROCEED` reports when any `Push-Feel Retest Focus` row is `No`; `HumanPlaytestTriageBuilder.gd` also routes failed retest rows to focused causes such as push camera peripheral readability, look-down control, hands/arm transition, or aim-control feel.
  - `VerticalSliceGateChecker.gd` now only counts `playtest-*.md` files and explicitly ignores `.triage.md`, so protocol docs, worksheets, and generated triage notes cannot inflate `invalid_or_automated_reports`. The refreshed readiness report is back to `INVALID_OR_AUTOMATED_REPORTS=8` with `VALID_HUMAN_REPORTS=0`.
  - `Milestone3HandoffBuilder.gd`, `tests/write_milestone3_handoff_report.gd`, `tests/test_milestone3_handoff_builder.gd`, and `tools/write_milestone3_handoff_report.sh` now generate `production/milestones/milestone3-alpha-handoff.md` from the same human gate scan. Current generated handoff is `LOCKED`: do not enter Alpha until `tools/check_milestone2_ready.sh` succeeds with a representative human `PROCEED` whose Human Feel Gate and Push-Feel Retest Focus are aligned; if unlocked later, the allowed work is limited to Alpha campaign skeleton planning, Level 1-7 route profiles using the existing ascent-release-descent loop, save/profile tracking, and tests preserving the Milestone 2 push baseline.
  - Computer Use visual verification launched the exported macOS app window, confirmed `MainMenu.tscn` renders first, clicked Play, confirmed `VerticalSlice.tscn` loads with mountain, stone, player, HUD, and telemetry, pressed Escape to show the pause menu, and clicked Resume to return to play.
  - `production/release/export-readiness.md` records the export state: Godot 4.6.2 export templates are installed, macOS export is generated and smoke-tested locally, and Windows/Linux remain target-machine validation work.
  - 2026-05-26 direct user feedback identified a new push-feel PIVOT: first-person transition arms could read too long, hands could visually enter the boulder, push aim lacked a stable center reference, peripheral direction read, and downward look, making intentional pushing unreliable. The qualitative note is recorded at `production/qa/playtests/user-feedback-2026-05-26-push-camera-hands.md`; it is intentionally not named `playtest-*.md` and does not count as milestone Human Feel Gate evidence.
  - Push-feel follow-up added tests for first-person forearm stretch, hand-vs-stone surface clearance, push-camera downward look, and center-reticle presence. `PushController.gd` now offsets visual palm targets outside the stone surface while preserving the physical contact point; `FirstPersonHandsController.gd` clamps forearm visual reach; `PlayerController.gd` allows lower push pitch and blends to a wider push FOV; `PushLab.tscn`, `Main.tscn`, and `VerticalSlice.tscn` now include a subtle center reticle.
  - Push-feel follow-up tightened the third-person/first-person hand transition and aim readability: first-person hands wait until the push camera is close enough, third-person arms hide early during push-camera blend, mouse/trackpad look can actually reach the lower push-contact view, biased push camera backs off for peripheral route judgment, push FOV is wider, and visual palms sit farther outside the boulder surface while contact force still uses the sphere contact point.
  - `test_vertical_slice_route_physics.gd` now prints per-segment start/end markers and accepts `--route-test=<name>` for isolated diagnosis (`near_ridge`, `front_route`, `representative_bias`, `push_segment`, `full_player_loop`, `descent_return`, `hud`). This was added after the full file appeared stuck with no progress output; the full suite now proves it was slow rather than failed, with `full_player_loop` as the expensive segment.
  - `PushLab.gd` now exposes `evaluate_aim_control_drill()`: from the same start it runs center/left/right short push trials and reports actual stone lateral delta, uphill gain, contact ratio, contact-side offset, side-force component, and spin ratio. `test_push_lab.gd` requires left/right aim to move the hand contact, apply side force, and produce opposite real stone drift while keeping uphill progress and sustained contact; this closes the previous gap where tests proved vectors changed but not that the boulder actually rolled in the aimed direction.
  - `ContactCue.gd` now provides a non-HUD world-space pressure cue: while push contact is valid, a small warm contact point and short force-direction bar appear at the boulder contact point; when push contact exits, it hides. `Player.tscn` includes the cue, `PlayerController.gd` drives it from the current `PushFrame`, and `PushLab.gd` reports cue visibility/location metrics for tests. This targets the user-reported readability issue: "I cannot judge exactly where I am pushing."
  - Latest push-feel iteration now targets the reticle-selected boulder surface instead of only mapping camera direction: `PushController.gd` accepts a camera origin and ray-tests the stone surface for `camera_contact_point`, while falling back to the stable contact model if the ray misses. `PlayerController.gd` passes the live camera origin, hides third-person arms immediately once push engages, avoids using world contact targets for visible third-person arms, and gives the push view more side-scan and downward look range. `FirstPersonHandsController.gd` shows first-person hands earlier in the transition and keeps palm centers outside the stone surface.
  - 2026-05-26 push-feel correction after direct user feedback: push view now hides the full third-person body as soon as first-person hands take over, first-person forearms are capped shorter and anchored in camera space to avoid long transition arms, push-camera FOV and downward look range are wider, default push camera is closer while biased aim backs off slightly for peripheral route judgment, palm targets sit farther outside the boulder, and `A/D` no longer dominates push force while camera aim adds a small side-step/pressure bias so left/right look creates real stone route pressure without pulling the player into the stone.
  - 2026-05-27 route readability follow-up: `MountainBuilder.gd` now generates non-colliding `RouteEdgeMarkers` on both sides of the mountain route. They sit outside the stone's clear corridor but inside peripheral view, with varied height/color so push view has environmental direction anchors instead of relying only on HUD/reticle. `test_game_logic.gd` verifies marker placement, height, surface alignment, and non-blocking corridor clearance; `test_push_lab.gd` verifies PushLab actually instantiates visible marker nodes.
  - 2026-05-27 descent contrast follow-up: `EnvironmentResponse.gd` now layers trail response instead of choosing one marker per trail point. Every pushed trail point creates a shallow `scar` layer, unstable sections add `water`, and stable daylight sections add `flower` or `grass`. `kind_counts()` exposes the layer mix for reports/tests. This directly supports the design promise from the source doc: the downhill walk should reveal that uphill labor changed the world.
  - 2026-05-27 descent composition follow-up: `VerticalSlice.gd` now frames the changed path during `descent` visual mode instead of using a generic third-person look target. `_focus_descent_camera_on_response()` centers the camera on the environment response cluster, opens the FOV for downhill observation, and `descent_response_view_status()` exposes visible response count, FOV, and nearest marker depth for tests/reports.
  - `Reticle surface targeting` is now an explicit human gate item. `PlaytestReportBuilder.gd` writes it into the F9 `Push-Feel Retest Focus` table, `HumanPlaytestReportValidator.gd` requires it, `HumanPlaytestTriageBuilder.gd` routes a failed row to reticle aim-control focus, and `tools/run_representative_playtest.sh` plus `representative-human-playtest-worksheet.md` print it before the representative run. `production/qa/playtests/README.md` now tells testers to fill both `Push-Feel Retest Focus` and `Human Feel Gate`.
  - `Milestone2ReadinessReportBuilder.gd` and `Milestone3HandoffBuilder.gd` now explicitly name `Reticle surface targeting` in Milestone 2 exit criteria and Milestone 3 entry conditions. Alpha remains locked if the center reticle cannot choose a readable boulder pressure point.
  - The representative human-report path now consistently tells testers to fill both `Push-Feel Retest Focus` and `Human Feel Gate`: `tools/run_representative_playtest.sh` prints both sections in the final run instruction, and `HumanPlaytestTriageBuilder.gd` routes invalid reports to fill every push-feel retest row, including `Reticle surface targeting`, instead of only naming the Human Feel Gate.
  - `tools/submit_representative_playtest_report.sh` now wraps the post-run human report submission path: validate the filled report, write focused triage, run the strict vertical-slice gate, refresh `production/qa/milestone2-readiness.md`, and refresh `production/milestones/milestone3-alpha-handoff.md`. `prepare_representative_playtest_packet.sh`, the worksheet, and the playtest README now point testers to this single submit command after F9 capture and manual field filling.
  - `VerticalSliceGateChecker.gd` and `tools/check_vertical_slice_gate.sh` now expose `INVALID_REPORT_SUMMARIES`, listing the first invalid/automated report filenames and concrete validation errors. `production/qa/milestone2-readiness.md` now persists the same diagnostics under `Invalid Report Diagnostics`, so the current `PIVOT` state is actionable instead of only showing an invalid count.
  - `Milestone2ReadinessReportBuilder.gd` now prints a `Recommended Human Run` block with exact next commands: preflight, representative run, `tools/submit_representative_playtest_report.sh`, and `tools/check_milestone2_ready.sh`, plus the expected F9 report path.
  - 2026-05-27 push-camera/hand iteration after direct user feedback: first-person hands now take over earlier during the third-person/first-person transition, third-person arms no longer chase world-space boulder targets during the transition, first-person forearms use shorter camera-space anchors, and palm safety projection was tightened so hands stay outside the boulder surface.
  - 2026-05-27 aim-control iteration: push camera placement now partially/fully anchors around the reticle-selected contact point in push view, PushLab uses the same resolved `PushFrame.aim_direction` for camera, hands, and contact cue, and `first_person_push_camera_status()` now reads `player.push_frame` plus a refreshed camera transform so automated evidence matches the actual rendered aim frame.
  - 2026-05-27 sustained biased push tests were added to prevent regressions where left/right/downward aim loses the boulder or the contact point from view. The current PushLab Computer Use check shows the boulder, route edge markers, pressure cue, and both hands visible during left-biased sustained push.
  - 2026-05-27 real VerticalSlice push-view follow-up: `VerticalSlice.gd` now exposes `push_view_status()` and uses the same two-pass reticle/contact/camera chain as PushLab for push visual modes. `PlayerController.gd` exposes the shared push-camera-origin helper for visual verification; live gameplay was later moved onto the same reticle-aligned solve after follow-up feedback. First-person hands initially took over at the start of push transition, before the later fix delayed them until the camera is close enough.
  - 2026-05-27 route physics repair after real route regression: smoke/default tuning now has enough centerline push envelope for the short vertical-slice route, lower default side-force authority to keep biased pushes recoverable, faster push-step speed so the player can maintain contact, and a small low-speed central assist in `PushController.gd` that only activates during valid hand contact near zero uphill velocity to break static friction/solver lock. The main contact driver remains `apply_force(contact_force, contact_offset)`.
  - 2026-05-27 follow-up after direct hand/camera feedback: live `PlayerController.gd` now uses the same predicted camera-origin / reticle-aligned push-frame solve as PushLab before applying contact force, so camera center, hand targets, pressure cue, and actual force share the same selected boulder surface. First-person hands now wait until the camera is close enough before appearing, avoiding long transition forearms; `FirstPersonHandsController.gd` projects palms back outside the boulder surface for high-left/high-right aim.
  - 2026-05-27 default vertical-slice push tuning was rebalanced after the reticle-aligned solve made the smoke route too smooth/fast: average uphill speed in the full player-loop diagnostic is now 0.876m/s with a 25.6% slow-labor ratio, restoring `Burden Gate PROCEED` while keeping left/right biased pushes recoverable.
  - `production/qa/playtests/playtest-2026-05-27-auto-route.md` was regenerated after the reticle-aligned live push fix: smoke profile completes in 51.5s with `Push Gate PROCEED`, `Burden Gate PROCEED`, `Slice Gate PIVOT`, `Push Lab Bias Gate PROCEED`, 100% contact, 1 contact loss, max spin ratio 2.0, average uphill speed 0.80m/s, slow-push duration 17.2s, response layers `{scar:84, water:1, grass:42, flower:41}`, max lateral offset 2.44, and bias recovered yes. This is a fast stability baseline, not representative proof.
  - `production/qa/playtests/playtest-2026-05-27-auto-route-representative-full.md` was regenerated after the same fixes: representative profile completes in 381.4s with `Push Gate PROCEED`, `Burden Gate PROCEED`, `Slice Gate PROCEED`, 100% contact, 1 contact loss, max spin ratio 1.7, uphill push distance 246.06m, average uphill speed 0.86m/s, slow-push duration 140.4s, ascent 286.3s, descent 94.5s, response layers `{scar:539, water:1, grass:269, flower:269}`, max lateral offset 1.26, and bias recovered yes. PushLab bias gate was intentionally skipped in this long run and remains covered by the smoke/PushLab evidence.
  - `tools/run_representative_playtest.sh --preflight manual-01 2026-05-27` passed after the latest push/camera fixes: F9 playtest capture tests passed, human report validator tests passed, readiness report was refreshed, and the helper printed the exact GUI run command plus the required `Push-Feel Retest Focus` checklist. `production/qa/playtests/representative-human-playtest-worksheet.md` and `production/qa/playtests/README.md` were updated to reference the 2026-05-27 reticle-aligned hand/camera/contact fixes and the current 2026-05-27 smoke/representative automated baselines.
  - 2026-05-27 direct push-feel feedback follow-up: first-person hands now only take over after the camera is physically close enough to the contact point, so blend value alone cannot create long transition arms. The first-person hand visual layer now uses camera-space left/right hand framing around the reticle-selected contact, short forearm stubs behind the palms, and a palm/anchor safety pass that keeps hand centers outside the boulder. Push view FOV is widened to 116, Up/Down arrow look fallback was added, and PushLab evidence now reports whether left/right route edge markers are visible in peripheral view. Fresh viewport evidence was regenerated at `/private/tmp/sisyphus-pushlab-core-sequence-test-left_bias.png` and `...-right_bias.png`.
  - 2026-05-27 vertical-slice parity follow-up: the same peripheral route-edge visibility check now exists in the real `VerticalSlice.push_view_status()`, not only in PushLab. `test_vertical_slice_visual_modes.gd` now requires left and right route-edge markers to be visible inside the actual push camera periphery while hands/contact cue/stone remain readable, preventing the representative playtest from passing a lab-only readability guard.
  - 2026-05-27 vertical-slice hand-contact parity follow-up: `VerticalSlice.push_view_status()` now reports `nearest_hand_to_contact` and `minimum_forearm_surface_clearance`, and `test_vertical_slice_visual_modes.gd` requires a palm to press near the selected contact point while forearm segments stay outside the boulder. This brings the real representative scene up to the same hand/contact safety standard as PushLab instead of relying on lab-only checks.
  - 2026-05-27 latest push-feel follow-up after direct user feedback: mid-blend push-camera transition now hides third-person arms before first-person hands are ready, preventing the "long arm" read during third/first-person switching. First-person hands are anchored farther forward in camera space, palm/anchor safety clearance is tightened, push-camera contact distance and FOV are widened for center-reference plus peripheral route judgment, and default side-force authority is rebalanced so aim-left/right remains visible but recoverable.
  - `VerticalSlice.gd` route smoke now uses the same two-pass reticle camera-origin solve as PushLab before applying contact push, so automated route evidence no longer uses a stale shoulder-camera origin after push-camera changes. Its bias window was reduced to create visible pressure without pushing the stone into side obstacles; `front_route`, `representative_bias`, and `full_player_loop` route tests pass after this correction.
  - 2026-05-27 push-intent diagnostic added: `PushLab.gd` now exposes `run_push_intent_diagnostic(base_path)`, `tests/run_push_intent_diagnostic.gd` and `tools/run_push_intent_diagnostic.sh` generate a short hand/contact/camera report plus six snapshots (`approach`, `center_push`, `left_high`, `right_high`, `look_down`, `disengage`). `test_push_intent_diagnostic.gd` gates the diagnostic contract and verifies contact delta, side-force delta, real stone drift gap, route-edge visibility, look-down readability, clean disengage, and hand-surface safety.
  - `production/qa/playtests/push-intent-diagnostic-2026-05-27-codex.md` records the current short diagnostic as `PROCEED`: contact delta `0.80`, force delta `88.03`, real drift gap `1.27`, left/right route visible, look-down readable, disengage clean, and hand surface clear. This means the short intent check supports doing the next representative human run; it explicitly does not replace a filled `Human Feel Gate`.
  - 2026-05-27 aim-readability follow-up after new direct feedback: push transition now hides third-person arms almost immediately after push engage, preventing the third/first-person blend from showing long reaching arms. The first-person push camera is backed off enough for peripheral route judgment while keeping the stone close, first-person hands preserve their reticle-selected contact targets instead of being flattened into a fixed symmetric pose, and headless push-intent snapshots now draw from real camera-space status so left/right/high/look-down evidence is visually distinguishable instead of only numerically different.
  - 2026-05-27 representative-playtest preflight now runs the short push-intent diagnostic automatically after F9/report contract checks and before writing readiness status. `tools/run_representative_playtest.sh --preflight manual-01 2026-05-27` generated `production/qa/playtests/push-intent-diagnostic-preflight-2026-05-27-manual-01.md` plus six PNGs, reported `PROCEED` with contact delta `0.80`, force delta `88.03`, and drift gap `1.27`, then printed the representative human run command. The diagnostic remains explicitly separate from the filled `Human Feel Gate`.
  - `Milestone2ReadinessReportBuilder.gd` now includes a `Push Intent Diagnostic Preflight` section in `production/qa/milestone2-readiness.md`. `tests/write_milestone2_readiness_report.gd` automatically selects the latest `push-intent-diagnostic-*.md`, extracts verdict, contact delta, force delta, and drift gap, and injects those into the readiness report while preserving the boundary that this diagnostic does not replace `Human Feel Gate`.
  - `tools/prepare_representative_playtest_packet.sh` now creates a per-tester human playtest packet after running preflight. For `manual-01 2026-05-27` it wrote `production/qa/playtests/playtest-2026-05-27-manual-01.packet.md`, linking the expected F9 report/screenshot, readiness report, preflight push-intent diagnostic and all six snapshots, the human run command, required fields to fill, validation/triage/gate commands, and PROCEED/PIVOT/KILL rules. `.packet.md` files are ignored by `VerticalSliceGateChecker.gd` so they do not inflate automated/invalid report counts.
  - 2026-05-27 gate-evidence cleanup: `Milestone2ReadinessReportBuilder.gd` now separates invalid human reports from automated baseline reports. Automated route/visual reports remain useful regression evidence, but no longer appear under invalid human report diagnostics as missing `Human Feel Gate`; strict milestone state remains `PIVOT` until a filled representative human report passes.
  - 2026-05-27 contact-camera coherence follow-up: push view camera origin now tracks the selected reticle contact point for composition while physics keeps using the stable body-locked contact ray, so visual framing no longer feeds back into unstable push force. `ContactCue.gd` shortens the normal pressure hint, and `FirstPersonHandsController.gd` now draws short wrist/forearm stubs near the palms instead of long first-person rods crossing the boulder. `design/quick-specs/push-view-reticle-camera-coherence-2026-05-27.md` records the design boundary.
  - 2026-05-27 human-gate update: `Push-Feel Retest Focus` now includes `Wrist/forearm silhouette`, requiring first-person wrist/forearm shapes to stay short and not read as rods crossing through the boulder. The row is enforced by the human report validator, F9 report template, worksheet, representative playtest packet, Milestone 2 readiness, and Milestone 3 handoff entry conditions.
  - 2026-05-27 aim-agency readability follow-up after direct feedback: `design/quick-specs/push-view-aim-agency-first-person-readability-2026-05-27.md` records the current correction. Live push solving now refines the contact ray from the actual push-camera origin, so the selected reticle surface, hand targets, pressure cue, and force are less likely to diverge. Push FOV is reduced from fisheye-style `122` to readable-wide `108`, first-person palms/forearms were enlarged and recolored with no-depth-test overlay materials, and forearm starts extend as short visible cuffs behind the palms so the hands read as pressing the stone rather than tiny dots or long rods.
  - 2026-05-27 representative QA handoff refreshed for `manual-02`: `tools/prepare_representative_playtest_packet.sh manual-02 2026-05-27` passed, generated `production/qa/playtests/playtest-2026-05-27-manual-02.packet.md`, refreshed `production/qa/milestone2-readiness.md`, and wrote `production/qa/playtests/push-intent-diagnostic-preflight-2026-05-27-manual-02.md` plus six snapshots. The latest preflight remains `PROCEED` with contact delta `0.80`, force delta `95.14`, and drift gap `1.63`.
  - 2026-05-27 readiness handoff fix: `Milestone2ReadinessReportBuilder.gd` now derives the recommended human-run tester/date from the latest `push-intent-diagnostic-preflight-YYYY-MM-DD-tester.md` report instead of hard-coding `manual-01`, so the refreshed readiness report now points to `manual-02` consistently.
  - 2026-05-27 Milestone 3 handoff was refreshed after the readiness fix. `production/milestones/milestone3-alpha-handoff.md` remains `LOCKED` / `PIVOT` and correctly allows no Alpha work until `tools/check_milestone2_ready.sh` succeeds with filled human evidence.
  - 2026-05-27 playtest protocol docs were synced to the current `manual-02` handoff. `production/qa/playtests/README.md` and `godot/README.md` now route testers through the current readiness report / per-tester packet instead of presenting stale `manual-01` examples as the active run path.
  - 2026-05-27 current-handoff wrapper added: `tools/run_current_representative_playtest.sh` reads the latest representative `.packet.md` as data, resolves tester/date, and delegates to `run_representative_playtest.sh`. `--show` previews the resolved command without opening Godot, and `--preflight` refreshes the current packet.
  - 2026-05-27 current-report submit wrapper added: `tools/submit_current_representative_playtest_report.sh` reads the latest representative `.packet.md` as data, resolves the expected F9 report path, and delegates to `submit_representative_playtest_report.sh`. `--show` previews the resolved report path without validating/submitting.
  - 2026-05-27 current-handoff status check added: `tools/check_current_representative_handoff.sh` validates the latest representative packet fields, push-intent preflight report, all six snapshots, and whether the expected F9 report/screenshot exists. Current output is `READY_FOR_HUMAN_RUN`: preflight evidence is present and `playtest-2026-05-27-manual-02.md` is still missing.
  - 2026-05-27 Milestone 2 readiness now embeds the current handoff status. `production/qa/milestone2-readiness.md` shows `READY_FOR_HUMAN_RUN`, links the active `manual-02` packet, names the missing expected F9 report, and prints `tools/run_current_representative_playtest.sh` as the next command.
  - 2026-05-27 Milestone 2 gate output now appends current handoff status when the strict human gate fails. `tools/check_milestone2_ready.sh --skip-tests` still exits with the strict gate status, but now prints `HANDOFF_STATUS=READY_FOR_HUMAN_RUN` and `NEXT_COMMAND=tools/run_current_representative_playtest.sh` after the `PIVOT` result.
  - 2026-05-27 representative packet commands were synced to the current wrappers. `tools/prepare_representative_playtest_packet.sh` and the active `playtest-2026-05-27-manual-02.packet.md` now show `tools/run_current_representative_playtest.sh` and `tools/submit_current_representative_playtest_report.sh` as the primary commands, with resolved concrete fallback commands preserved for reproducibility.
  - 2026-05-27 Milestone 2 readiness recommended-run section now also prefers current wrappers: `tools/check_current_representative_handoff.sh`, `tools/run_current_representative_playtest.sh`, `tools/submit_current_representative_playtest_report.sh`, then `tools/check_milestone2_ready.sh`; resolved fallback commands for `manual-02 2026-05-27` remain listed below.
- Still not complete:
  - The vertical slice now has an automated 5-10 minute representative `PROCEED` route plus manual report/gate tooling, but `tools/check_milestone2_ready.sh` still fails until a filled human playtest report validates the feel.
  - Visual verification covers automated states, push-lab state HUD observation, a close first-person push-lab observation camera, a subtle first-person hand cue path, a short-loop driver, a bot-driven full-slope route smoke, automated player-input route check, HUD telemetry, an automated representative `PROCEED` route, and a representative pacing profile, not a completed human-validated 5-10 minute playthrough.
  - No filled human playtest report with `Human Verdict` has been captured yet.
  - First-pass generated hum playback and chapter-end audio relationship exist, but final audio direction, mix, and arrangement are not done.
  - Steam release work is only at export-readiness baseline; export templates, signed desktop builds, SteamPipe/depot setup, store assets, and external release QA are still pending.
- Last verification: 2026-05-27, after the aim-agency readability follow-up and `manual-02` QA packet refresh, `test_push_lab.gd`, `test_game_logic.gd`, `test_contact_push_physics.gd`, `test_player_behavior.gd`, `test_vertical_slice_visual_modes.gd`, `test_push_physics.gd`, `test_main_start.gd`, `test_push_intent_diagnostic.gd`, `test_vertical_slice_route_physics.gd -- --route-test=representative_bias`, and `test_vertical_slice_route_physics.gd -- --route-test=full_player_loop` passed. `tools/prepare_representative_playtest_packet.sh manual-02 2026-05-27` passed and generated the latest human-run packet plus push-intent preflight diagnostic (`PROCEED`, contact delta `0.80`, force delta `95.14`, drift gap `1.63`). The readiness handoff fix passed `test_milestone2_readiness_report.gd`; `tools/write_milestone2_readiness_report.sh` regenerated `production/qa/milestone2-readiness.md` with current wrappers plus resolved `manual-02` fallback commands; `tools/check_milestone2_ready.sh --skip-tests` correctly returned `PIVOT`, `VALID_HUMAN_REPORTS=0`, `AUTOMATED_BASELINE_REPORTS=10`, and appended the current handoff status; `test_milestone3_handoff_builder.gd` passed and `tools/write_milestone3_handoff_report.sh` refreshed the Alpha handoff as `LOCKED`; `test_release_tooling.gd` passed after syncing README handoff commands, adding current run/submit/status wrapper contracts, requiring Milestone 2 gate output to include current handoff status, and requiring packets to prefer current wrappers with resolved fallback commands; `bash -n tools/prepare_representative_playtest_packet.sh`, `bash -n tools/run_current_representative_playtest.sh`, `bash -n tools/submit_current_representative_playtest_report.sh`, `bash -n tools/check_current_representative_handoff.sh`, and `bash -n tools/check_milestone2_ready.sh` passed; `test_milestone2_readiness_report.gd` passed after adding the embedded handoff-status section and wrapper-first recommended run; `tools/write_milestone2_readiness_report.sh` regenerated `production/qa/milestone2-readiness.md` with `HANDOFF_STATUS=READY_FOR_HUMAN_RUN`; `tools/run_current_representative_playtest.sh --show` resolved the current packet to `manual-02 2026-05-27`; `tools/submit_current_representative_playtest_report.sh --show` resolved the expected F9 report to `playtest-2026-05-27-manual-02.md`; `tools/check_current_representative_handoff.sh` returned `HANDOFF_STATUS=READY_FOR_HUMAN_RUN` and `NEXT_COMMAND=tools/run_current_representative_playtest.sh`. Computer Use on live `PushLab.tscn -- --lab-auto=left` and `--lab-auto=center` confirmed the rendered push view now shows readable foreground palms/short forearm cuffs, the center contact cue, and peripheral route/obstacle anchors. Current gate state remains `PIVOT` until a filled representative human playtest report validates the feel.
